import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hi")
            Image(systemName: "photo.artframe")
                
                
            
                
        }
    }
}
